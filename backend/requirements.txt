# Version ranges rather than exact pins: RTC is expected to build on Python 3.11-3.14,
# and several C-extension packages only publish wheels for the newest interpreters at
# their latest patch level. `requirements.lock.txt` (generated per environment with
# `pip freeze`) is what deployments should install from.

# --- Web framework ---------------------------------------------------------
fastapi>=0.115,<0.121
uvicorn[standard]>=0.34,<0.40
python-multipart>=0.0.20

# --- Data ------------------------------------------------------------------
SQLAlchemy>=2.0.36,<2.1
alembic>=1.14,<2.0
psycopg[binary]>=3.2.10,<4.0        # PostgreSQL driver (production)
pydantic[email]>=2.10,<3.0
pydantic-settings>=2.7,<3.0

# --- Security --------------------------------------------------------------
pyjwt>=2.10,<3.0
argon2-cffi>=23.1
pyotp>=2.9
cryptography>=44.0

# --- Object storage --------------------------------------------------------
boto3>=1.43.58

# --- AI-assisted authoring -------------------------------------------------
# Optional at runtime. With RTC_ENABLE_AI_GENERATION unset the platform uses a
# deterministic offline provider and never imports this, so a deployment that
# does not want AI features can drop the line. It is listed here so CI installs
# it and the Anthropic code path is type-checked and importable.
anthropic>=0.69

# --- Utilities -------------------------------------------------------------
python-dateutil>=2.9
httpx>=0.28

# --- Testing ---------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.25
